# Dependencies
node_modules/
.pnp
.pnp.js
# Not committed: an npm-version-specific lockfile crashed `npm install` on
# older npm (10.x) with "Exit handler never called". We use `npm install`
# (not `npm ci`) everywhere, so a fresh resolve keeps every environment working.
package-lock.json

# Builds
dist/
build/
.next/
out/
*.tsbuildinfo

# Env
.env
.env.local
.env.*.local
!.env.example

# Logs
logs/
*.log
npm-debug.log*

# Data / runtime (root-level only — anchored so they don't match source dirs of
# the same name, e.g. api/.../servers/[id]/backups/)
/data/
/volumes/
/backups/
/var/

# Prisma
apps/panel/prisma/*.db

# Go build artifacts
*.exe
apps/edge-proxy/mgg-edge-proxy
apps/edge-proxy/edge-proxy
apps/edge-proxy/config.json

# OS / editors
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
.sixth/

# Secrets
*.pem
daemon.token
node-config.json
